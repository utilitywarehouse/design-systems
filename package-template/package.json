{
  "name": "@utilitywarehouse/customer-ui-{{package.name}}",
  "version": "0.1.0",
  "description": "{{package.description}}",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist/",
    "clean:deps": "rimraf node_modules",
    "build": "pnpm clean && tsc",
    "watch": "pnpm build -- --watch",
    "test": "jest"
  },
  "license": "ISC",
  "publishConfig": {
    "access": "restricted"
  },
  "files": [
    "dist",
    "src"
  ]
}
