{
  "installCommand": "pnpm install",
  "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider pnpm build && NODE_OPTIONS=--openssl-legacy-provider pnpm build:storybook",
  "outputDirectory": "packages/web-ui/storybook-static"
}
