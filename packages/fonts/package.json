{
  "name": "@utilitywarehouse/customer-ui-fonts",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean:dist": "rimraf fonts.css*",
    "clean:deps": "rimraf node_modules",
    "clean": "pnpm clean:dist && pnpm clean:deps",
    "build": "pnpm clean:dist && sass fonts.scss fonts.css",
    "watch": "pnpm build -- --watch"
  },
  "license": "ISC",
  "publishConfig": {
    "access": "restricted"
  },
  "files": [
    "*.ttf",
    "*.woff*",
    "*.scss",
    "*.css*"
  ],
  "devDependencies": {
    "sass": "^1.32.6"
  }
}
