{
  "name": "@utilitywarehouse/customer-ui-react-icons",
  "version": "0.1.0",
  "description": "React icons based on the icons svgs",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist/",
    "build": "node ./scripts/build.js && yarn run clean && tsc",
    "watch": "echo \"Nothing to watch\"",
    "test": "echo \"Nothing to test\"",
    "ci-package-changed": "exit 0"
  },
  "license": "ISC",
  "publishConfig": {
    "access": "restricted"
  },
  "devDependencies": {
    "@svgr/core": "^5.5.0",
    "@svgr/cli": "^5.5.0",
    "@utilitywarehouse/customer-ui-icons": "^1.0.0-alpha.3",
    "mkdirp": "^1.0.4",
    "rimraf": "^3.0.2",
    "uuid": "^8.3.2"
  },
  "files": [
    "dist",
    "src"
  ]
}
