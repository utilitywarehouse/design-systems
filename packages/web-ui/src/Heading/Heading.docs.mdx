import { Meta, Canvas, ArgTypes, Typeset } from '@storybook/blocks';
import * as Stories from './Heading.stories';
import { DocsHeader } from '../storybook-utils';

<Meta of={Stories} />

<DocsHeader
  componentName="Heading"
  stories={Stories}
  figmaLink="https://www.figma.com/design/4FFYTLWJ2hQpj36JplQQUw/UW-Web-UI?node-id=38-884"
/>

<Canvas of={Stories.KitchenSink} sourceState="none" />

- [Alternatives](#alternatives)
- [Variants](#variants)
- [Color prop](#color-prop)
  - [Contextual colour](#contextual-colour)
- [Props](#props)

## Alternatives

- [Text](?path=/docs/components-text--documentation) - For body text

## Variants

<Canvas of={Stories.HeadingVariants} />

## Color prop

You can pass any valid string color to the `Heading` component. It is recommended
you use the `@utilitywarehouse/colour-system` package, and pass color values
from that into the component. The default color is `colorsCommon.brandPrimaryPurple`
from this package.

```tsx
import { colorsCommon } from '@utilitywarehouse/colour-system'

<Heading color={colorsCommon.brandMidnight}>{...}</Heading>
```

### Contextual colour

When inside a `Box` component that specifies a `backgroundColor` which is the
value of either `colorsCommon.brandMidnight` or
`colorsCommon.brandPrimaryPurple`, the `Heading` color will be set to
`colorsCommon.brandWhite`. This will be overridden by the `color` prop.

<Canvas of={Stories.HeadingColour} />

## Props

<ArgTypes of={Stories} />
