import { Meta } from '@storybook/blocks';
import { breakpoints } from '../../../src/tokens';
import { Box, Stack, Text } from '../../../src';
import { Divider } from '@mui/material';

<Meta title="Web UI / Tokens / breakpoints" />

# Breakpoints

<Stack spacing={1} component="ul" padding={0} margin={0} width={200} divider={<Divider />}>
  {Object.keys(breakpoints).map(bp => (
    <Box component="li" display="flex" justifyContent="space-between">
      <Text variant="body" component="span">
        {bp}
      </Text>
      <Text variant="body" component="span">
        {breakpoints[bp]}px
      </Text>
    </Box>
  ))}
</Stack>
