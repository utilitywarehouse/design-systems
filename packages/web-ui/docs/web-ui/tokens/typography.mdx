import { Meta } from '@storybook/blocks';
import { fonts, fontWeights } from '../../../src/tokens';
import { Box, Stack, Text } from '../../../src';
import { Divider } from '@mui/material';

<Meta title="Web UI / Tokens / typography" />

# Font families

<Stack spacing={1} width={400} divider={<Divider />}>
  {Object.keys(fonts).map(font => (
    <Box display="flex" justifyContent="space-between">
      <Text variant="body" component="span">
        {`fonts.${font}`}
      </Text>
      <Text variant="body" component="span">
        {fonts[font]}
      </Text>
    </Box>
  ))}
</Stack>

# Font weights

<Stack spacing={1} width={400} divider={<Divider />}>
  <Box display="flex" justifyContent="space-between" alignItems="center">
    <Text variant="body" component="span">
      {`fontWeights.primary`}
    </Text>
    <Text variant="body" component="span">
      {fontWeights.primary}
    </Text>
  </Box>
  <Box display="flex" justifyContent="space-between" alignItems="center">
    <Text variant="body" component="span">
      {`fontWeights.secondary.regular`}
    </Text>
    <Text variant="body" component="span">
      {fontWeights.secondary.regular}
    </Text>
  </Box>
  <Box display="flex" justifyContent="space-between" alignItems="center">
    <Text variant="body" component="span">
      {`fontWeights.secondary.semibold`}
    </Text>
    <Text variant="body" component="span">
      {fontWeights.secondary.semibold}
    </Text>
  </Box>
</Stack>
