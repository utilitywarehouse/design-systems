import { Meta } from '@storybook/blocks';
import { colors, colorsCommon } from '@utilitywarehouse/colour-system';
import { Box, Stack, Text, Heading } from '../../src';

<Meta title="Colour System / Common" />

# Common Colours

      <Stack spacing={2}  component="ul" padding={0} margin={0}  >
      {Object.keys(colorsCommon).map((color)=> {
      return(
      <Box key={color} display='flex' justifyContent='space-between' alignItems='center' >
      <Box width={100} height={100} bgcolor={colorsCommon[color]}></Box>
      <Box flex={2} display='flex' justifyContent='center'>
          <Text component='span' variant='body' bold >
          {color}
          </Text>
      </Box>

      <Text component='span' variant='body' flex={3}>

      </Text>

      </Box>
      ) })}

    </Stack>
