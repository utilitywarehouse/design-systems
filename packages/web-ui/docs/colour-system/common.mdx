import { Meta } from '@storybook/blocks';
import { colorsCommon } from '@utilitywarehouse/colour-system';
import { Box, Text, Heading } from '../../src';
import { ColourDisplay } from '../components/ColourDisplay.tsx';

<Meta title="Colour System / Common" />

# Common Colours

      <Box display='flex' flexDirection='column' gap={2}  component="ul" padding={0} margin={0}  >
      {Object.keys(colorsCommon).map((color)=> {
      return(
      <Box key={color} display='flex' gap={4} alignItems='center' >
      <Box width={100} height={100} bgcolor={colorsCommon[color]}></Box>
      <Box  display='flex' justifyContent='center'>
          <Text  variant='body' bold >
          {color}
          </Text>
      </Box>

      <Text  variant='body' >

      </Text>

      </Box>
      ) })}

    </Box>
