import { styled, Text } from '@gluestack-ui/themed';

const ListHeadingSupportingText = styled(
  Text,
  {},
  {
    componentName: 'ListHeadingSupportingText',
    descendantStyle: [],
    ancestorStyle: ['_supportingText'],
  }
);

export default ListHeadingSupportingText;
