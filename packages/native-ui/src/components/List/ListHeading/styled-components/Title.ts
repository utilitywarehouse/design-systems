import { styled, Heading } from '@gluestack-ui/themed';

const ListHeading = styled(
  Heading,
  {},
  {
    componentName: 'ListHeadingTitle',
    descendantStyle: [],
    ancestorStyle: ['_title'],
  }
);

export default ListHeading;
