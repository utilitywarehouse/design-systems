import { Meta, Canvas, Story, Controls } from '@storybook/blocks';
import * as Stories from './List.stories';
import {
  ViewFigmaButton,
  BackToTopButton,
  UsageWrap,
  CheckboxList,
  BadgeList,
  RadioList,
  SwitchList,
} from '../../../docs/components';
import {
  NativeUIProvider,
  List,
  ListHeading,
  ListItem,
  ListItemIcon,
  ListItemLeadingContent,
  ListItemSupportingText,
  ListItemText,
  ListItemTrailingContent,
  ListItemTrailingIcon,
  ListItemContent,
  Box,
  Badge,
  BadgeText,
  HStack,
  Checkbox,
  CheckboxGroup,
  CheckboxIndicator,
  FlatList,
  Card,
  Image,
} from '../../';
import {
  BillMediumIcon,
  ChevronRight01MediumIcon,
  PaymentMediumIcon,
  HomeMediumIcon,
  UserMediumIcon,
  ElectricityMediumIcon,
  GasMediumIcon,
} from '@utilitywarehouse/react-native-icons';

<Meta title="Components / List" />

<ViewFigmaButton url="https://www.figma.com/design/3RY3OvLA88yZksRjOfjQJm/UW-App-UI?node-id=2692-3920&m=dev" />

<BackToTopButton />

# List

The List component serves as the primary wrapper for List Items and List Heading components.

List item is the main building block of list. Each item has its own set of properties that you can customise and adapt to suit your needs.

- [Playground](#playground)
- [Usage](#usage)
- [Props](#props)
  - [`List`](#list-1)
  - [`ListHeading`](#listheading)
  - [`ListItem`](#listitem)
- [Advanced Usage](#advanced-usage)
- [Variants](#variants)
- [Examples](#examples)
  - [List Item with Card](#list-item-with-card)
  - [List Item with Badge](#list-item-with-badge)
  - [Checbox List](#checbox-list)
  - [Radio List](#radio-list)
  - [Switch List](#switch-list)
  - [List with FlatList](#list-with-flatlist)

## Playground

<Canvas of={Stories.Playground} />

<Controls of={Stories.Playground} />

## Usage

<UsageWrap>
  <List headingText="Heading" headingSupportingText="Supporting text" divider>
    <ListItem text="List Item 1" onPress={() => console.log('item pressed')} />
    <ListItem text="List Item 2" onPress={() => console.log('item pressed')} />
    <ListItem text="List Item 3" onPress={() => console.log('item pressed')} />
  </List>
</UsageWrap>

```tsx
import { List, ListItem } from '@utilitywarehouse/native-ui';

const MyComponent = () => (
  <List headingText="Heading" headingSupportingText="Supporting text" divider>
    <ListItem text="List Item 1" onPress={() => console.log('item pressed')} />
    <ListItem text="List Item 2" onPress={() => console.log('item pressed')} />
    <ListItem text="List Item 3" onPress={() => console.log('item pressed')} />
  </List>
);
```

## Props

### `List`

| Name                  | Type                | Default  | Description                                                |
| --------------------- | ------------------- | -------- | ---------------------------------------------------------- |
| container             | `'full' \| 'card' ` | `'full'` | The container style of the list.                           |
| headingText           | string              |          | The text to display in the heading of the list.            |
| headingSupportingText | string              |          | The supporting text to display in the heading of the list. |
| divider               | boolean             | false    | Whether to display a divider below the list items.         |
| dividerColor          | string              |          | The color of the divider.                                  |
| loading               | boolean             | false    | Whether to show the list items in loading state.           |
| disabled              | boolean             | false    | Whether to disable the list.                               |

### `ListHeading`

| Name           | Type   | Default | Description                                                |
| -------------- | ------ | ------- | ---------------------------------------------------------- |
| text           | string |         | The text to display in the heading of the list.            |
| supportingText | string |         | The supporting text to display in the heading of the list. |

#### - `ListHeadingText`

#### - `ListHeadingSupportingText`

### `ListItem`

| Name            | Type       | Default | Description                                                     |
| --------------- | ---------- | ------- | --------------------------------------------------------------- |
| text            | string     |         | The text to display in the list item.                           |
| onPress         | () => void |         | A callback function to be called when the list item is pressed. |
| supportingText  | string     |         | The supporting text to display in the list item.                |
| leadingContent  | ReactNode  |         | The leading content to display in the list item.                |
| trailingContent | ReactNode  |         | The trailing content to display in the list item.               |
| disabled        | boolean    | false   | Whether to disable the list item.                               |
| loading         | boolean    | false   | Whether to show the list item in loading state.                 |
| divider         | boolean    | false   | Whether to display a divider below the list item.               |
| dividerColor    | string     |         | The color of the divider.                                       |

#### - `ListItemLeadingContent`

#### - `ListItemIcon`

| Name | Type     | Default | Description                                     |
| ---- | -------- | ------- | ----------------------------------------------- |
| as   | React.FC |         | The icon component to display in the list item. |

#### - `ListItemContent`

#### - `ListItemText`

#### - `ListItemSupportingText`

#### - `ListItemTrailingContent`

#### - `ListItemTrailingIcon`

| Name | Type     | Default | Description                                     |
| ---- | -------- | ------- | ----------------------------------------------- |
| as   | React.FC |         | The icon component to display in the list item. |

## Advanced Usage

If you need to use the List component in a more advanced way, you can use the parts of the component directly.

<UsageWrap>
  <List>
    <ListHeading text="Your account" supportingText="Tap the links below to view your account" />
    <ListItem onPress={() => console.log('pressed')} divider>
      <ListItemLeadingContent>
        <ListItemIcon as={BillMediumIcon} />
      </ListItemLeadingContent>
      <ListItemContent>
        <ListItemText>Bills</ListItemText>
        <ListItemSupportingText>View your bills</ListItemSupportingText>
      </ListItemContent>
      <ListItemTrailingContent>
        <ListItemTrailingIcon as={ChevronRight01MediumIcon} />
      </ListItemTrailingContent>
    </ListItem>
    <ListItem onPress={() => console.log('pressed')} divider>
      <ListItemLeadingContent>
        <ListItemIcon as={PaymentMediumIcon} />
      </ListItemLeadingContent>
      <ListItemContent>
        <ListItemText>Payments</ListItemText>
        <ListItemSupportingText>Make a payment</ListItemSupportingText>
      </ListItemContent>
      <ListItemTrailingContent>
        <ListItemTrailingIcon as={ChevronRight01MediumIcon} />
      </ListItemTrailingContent>
    </ListItem>
    <ListItem onPress={() => console.log('pressed')} divider>
      <ListItemLeadingContent>
        <ListItemIcon as={HomeMediumIcon} />
      </ListItemLeadingContent>
      <ListItemContent>
        <ListItemText>Moving home</ListItemText>
        <ListItemSupportingText>Tell us if your moving</ListItemSupportingText>
      </ListItemContent>
      <ListItemTrailingContent>
        <ListItemTrailingIcon as={ChevronRight01MediumIcon} />
      </ListItemTrailingContent>
    </ListItem>
    <ListItem onPress={() => console.log('pressed')}>
      <ListItemLeadingContent>
        <ListItemIcon as={UserMediumIcon} />
      </ListItemLeadingContent>
      <ListItemContent>
        <ListItemText>Refer a friend</ListItemText>
        <ListItemSupportingText>Get rewarded with a friend</ListItemSupportingText>
      </ListItemContent>
      <ListItemTrailingContent>
        <ListItemTrailingIcon as={ChevronRight01MediumIcon} />
      </ListItemTrailingContent>
    </ListItem>
  </List>
</UsageWrap>

```tsx
import {
  List,
  ListHeading,
  ListItem,
  ListItemIcon,
  ListItemLeadingContent,
  ListItemSupportingText,
  ListItemText,
  ListItemTrailingContent,
  ListItemTrailingIcon,
  ListItemContent,
} from '@utilitywarehouse/native-ui';
import {
  BillMediumIcon,
  ChevronRight01MediumIcon,
  PaymentMediumIcon,
  HomeMediumIcon,
  UserMediumIcon,
} from '@utilitywarehouse/react-native-icons';

const MyComponent = () => (
  <List>
    <ListHeading text="Your account" supportingText="Tap the links below to view your account" />
    <ListItem onPress={() => console.log('pressed')} divider>
      <ListItemLeadingContent>
        <ListItemIcon as={BillMediumIcon} color="$pink300" />
      </ListItemLeadingContent>
      <ListItemContent>
        <ListItemText>Bills</ListItemText>
        <ListItemSupportingText>View your bills</ListItemSupportingText>
      </ListItemContent>
      <ListItemTrailingContent>
        <ListItemTrailingIcon as={ChevronRight01MediumIcon} />
      </ListItemTrailingContent>
    </ListItem>
    <ListItem onPress={() => console.log('pressed')} divider>
      <ListItemLeadingContent>
        <ListItemIcon as={PaymentMediumIcon} color="$pink300" />
      </ListItemLeadingContent>
      <ListItemContent>
        <ListItemText>Payments</ListItemText>
        <ListItemSupportingText>Make a payment</ListItemSupportingText>
      </ListItemContent>
      <ListItemTrailingContent>
        <ListItemTrailingIcon as={ChevronRight01MediumIcon} />
      </ListItemTrailingContent>
    </ListItem>
    <ListItem onPress={() => console.log('pressed')} divider>
      <ListItemLeadingContent>
        <ListItemIcon as={HomeMediumIcon} color="$pink300" />
      </ListItemLeadingContent>
      <ListItemContent>
        <ListItemText>Moving home</ListItemText>
        <ListItemSupportingText>Tell us if your moving</ListItemSupportingText>
      </ListItemContent>
      <ListItemTrailingContent>
        <ListItemTrailingIcon as={ChevronRight01MediumIcon} />
      </ListItemTrailingContent>
    </ListItem>
    <ListItem onPress={() => console.log('pressed')}>
      <ListItemLeadingContent>
        <ListItemIcon as={UserMediumIcon} color="$pink300" />
      </ListItemLeadingContent>
      <ListItemContent>
        <ListItemText>Refer a friend</ListItemText>
        <ListItemSupportingText>Get rewarded with a friend</ListItemSupportingText>
      </ListItemContent>
      <ListItemTrailingContent>
        <ListItemTrailingIcon as={ChevronRight01MediumIcon} />
      </ListItemTrailingContent>
    </ListItem>
  </List>
);
```

## Variants

The `List` component has different ways to display the list.

<Canvas of={Stories.Variants} />

## Examples

### List Item with Card

You can use the `Card` component to wrap the list items and setting the `container` prop to `card`.

<UsageWrap>
  <List container="card">
    <Card variant="outline" padding="none">
      <ListItem
        leadingContent={<BillMediumIcon />}
        text="Bills"
        divider
        onPress={() => console.log('item pressed')}
      />
      <ListItem
        leadingContent={<PaymentMediumIcon />}
        text="Payments"
        divider
        onPress={() => console.log('item pressed')}
      />
      <ListItem
        leadingContent={<HomeMediumIcon />}
        text="Moving Home"
        onPress={() => console.log('item pressed')}
      />
    </Card>
  </List>
</UsageWrap>

```tsx
import { List, ListItem, Card } from '@utilitywarehouse/native-ui';
import {
  BillMediumIcon,
  PaymentMediumIcon,
  HomeMediumIcon,
} from '@utilitywarehouse/react-native-icons';

const MyComponent = () => (
  <List container="card">
    <Card variant="outline" padding="none">
      <ListItem
        leadingContent={<BillMediumIcon />}
        text="Bills"
        divider
        onPress={() => console.log('item pressed')}
      />
      <ListItem
        leadingContent={<PaymentMediumIcon />}
        text="Payments"
        divider
        onPress={() => console.log('item pressed')}
      />
      <ListItem
        leadingContent={<HomeMediumIcon />}
        text="Moving Home"
        onPress={() => console.log('item pressed')}
      />
    </Card>
  </List>
);
```

### List Item with Badge

You can use the `Badge` component to display additional information in the list item. You can achieve this by using the List Item's
parts like `ListItemLeadingContent`, `ListItemContent`, and `ListItemTrailingContent` components.

<UsageWrap>
  <BadgeList />
</UsageWrap>

```tsx
import {
  List,
  ListItem,
  ListItemIcon,
  ListItemLeadingContent,
  ListItemSupportingText,
  ListItemText,
  ListItemTrailingContent,
  ListItemTrailingIcon,
  ListItemContent,
  Badge,
  BadgeText,
  HStack,
  useColorMode,
} from '@utilitywarehouse/native-ui';

import {
  ChevronRight01MediumIcon,
  ElectricityMediumIcon,
  GasMediumIcon,
} from '@utilitywarehouse/react-native-icons';
import { Box } from '@utilitywarehouse/native-ui/lab';

const BadgeList = () => {
  const colorMode = useColorMode();
  return (
    <List>
      <ListItem onPress={() => console.log('pressed')} divider>
        <ListItemLeadingContent>
          <Box
            padding="$3"
            borderRadius="$lg"
            bg={colorMode === 'light' ? '$apple50' : '$apple800'}
          >
            <ListItemIcon
              as={ElectricityMediumIcon}
              color={colorMode === 'light' ? '$apple800' : '$apple200'}
            />
          </Box>
        </ListItemLeadingContent>
        <ListItemContent>
          <ListItemText>Electricity</ListItemText>
          <Badge colorScheme="cyan" size="small">
            <BadgeText>Text</BadgeText>
          </Badge>
          <ListItemSupportingText>Last reading 23/03/24</ListItemSupportingText>
        </ListItemContent>
        <ListItemTrailingContent>
          <ListItemTrailingIcon as={ChevronRight01MediumIcon} />
        </ListItemTrailingContent>
      </ListItem>
      <ListItem onPress={() => console.log('pressed')} divider={false}>
        <ListItemLeadingContent>
          <Box
            padding="$3"
            borderRadius="$lg"
            bg={colorMode === 'light' ? '$apple50' : '$apple800'}
          >
            <ListItemIcon
              as={GasMediumIcon}
              color={colorMode === 'light' ? '$apple800' : '$apple200'}
            />
          </Box>
        </ListItemLeadingContent>
        <ListItemContent>
          <HStack style={{ justifyContent: 'space-between' }}>
            <ListItemText>Electricity</ListItemText>
            <Badge colorScheme="green" size="small">
              <BadgeText>Smart meter</BadgeText>
            </Badge>
          </HStack>
          <ListItemSupportingText>Last reading 23/03/24</ListItemSupportingText>
        </ListItemContent>
        <ListItemTrailingContent>
          <ListItemTrailingIcon as={ChevronRight01MediumIcon} />
        </ListItemTrailingContent>
      </ListItem>
    </List>
  );
};
```

### Checbox List

You can also use the list to display a list of checkboxes. You can achieve this by using the `Checkbox` component as the `trailingContent` of the `ListItem`.

<UsageWrap>
  <CheckboxList />
</UsageWrap>

```tsx
import { List, ListItem, ListItemIcon, Checkbox, CheckboxGroup } from '@utilitywarehouse/native-ui';
import { UserMediumIcon } from '@utilitywarehouse/react-native-icons';

const CheckboxList = () => {
  const [values, setValues] = React.useState<Array<string>>([]);

  const handlePress = (item: string) => {
    if (values.includes(item)) {
      setValues(values.filter(value => value !== item));
    } else {
      setValues([...values, item]);
    }
  };

  const handleChange = (values: Array<string>) => {
    setValues(values);
  };

  return (
    <CheckboxGroup
      aria-label="Checkbox Group"
      nativeID="checkbox-group"
      onChange={handleChange}
      value={values}
    >
      <List>
        <ListItem
          text="Joe Blogs"
          supportingText="12 Somewhere Street"
          leadingContent={<ListItemIcon as={UserMediumIcon} />}
          trailingContent={<Checkbox value="option1" aria-label="Label 1" />}
          onPress={() => handlePress('option1')}
        />
        <ListItem
          text="Mary Poppins"
          supportingText="45 Somewhere Street"
          leadingContent={<ListItemIcon as={UserMediumIcon} />}
          trailingContent={<Checkbox value="option2" aria-label="Label 2" />}
          onPress={() => handlePress('option2')}
        />
        <ListItem
          text="Charlie Brown"
          supportingText="78 Somewhere Street"
          leadingContent={<ListItemIcon as={UserMediumIcon} />}
          trailingContent={<Checkbox value="option3" aria-label="Label 3" />}
          onPress={() => handlePress('option3')}
        />
        <ListItem
          text="Jane Doe"
          supportingText="90 Somewhere Street"
          leadingContent={<ListItemIcon as={UserMediumIcon} />}
          trailingContent={<Checkbox value="option4" aria-label="Label 4" />}
          onPress={() => handlePress('option4')}
        />
      </List>
    </CheckboxGroup>
  );
};
```

For more information on the `Checbox` component, see the [`Checkbox` component documentation](/docs/native-ui-components-checkbox--docs).

### Radio List

You can also use the list to display a list of radio buttons. You can achieve this by using the `Checkbox` component as the `trailingContent` of the `ListItem`.

<UsageWrap>
  <RadioList />
</UsageWrap>

```tsx
import {
  List,
  ListItem,
  Card,
  Radio,
  RadioGroup,
  Image,
  useColorMode,
} from '@utilitywarehouse/native-ui';
import { Box } from '@utilitywarehouse/native-ui/lab';

const capitalize = (s: string) => s.charAt(0).toUpperCase() + s.slice(1);

const RadioList = () => {
  const [value, setValue] = useState<string>();
  const colorMode = useColorMode();

  const handlePress = (item: string) => {
    setValue(item);
  };

  const handleChange = (value: string) => {
    setValue(value);
  };

  return (
    <Box p="$4" backgroundColor={colorMode === 'light' ? '#F2F2F2' : '$grey25'}>
      <RadioGroup
        aria-label="Radio Group"
        nativeID="Radio-group"
        onChange={handleChange}
        value={value}
      >
        <List container="card">
          <Card variant="filled" padding="none">
            <ListItem
              text="**** 5016"
              supportingText="Expires 12/24"
              divider
              leadingContent={
                <Image
                  source={{ uri: `/images/paymentMethods/visa/card${capitalize(colorMode)}.svg` }}
                  width={56}
                  height={42}
                />
              }
              trailingContent={<Radio value="option1" aria-label="Label 1" />}
              onPress={() => handlePress('option1')}
            />
            <ListItem
              text="**** 3786"
              supportingText="Expires 04/27"
              leadingContent={
                <Image
                  source={{
                    uri: `/images/paymentMethods/mastercard/card${capitalize(colorMode)}.svg`,
                  }}
                  width={56}
                  height={42}
                />
              }
              divider
              trailingContent={<Radio value="option2" aria-label="Label 2" />}
              onPress={() => handlePress('option2')}
            />
            <ListItem
              text="**** 1234"
              supportingText="Expires 06/25"
              leadingContent={
                <Image
                  source={{
                    uri: `/images/paymentMethods/mastercard/card${capitalize(colorMode)}.svg`,
                  }}
                  width={56}
                  height={42}
                />
              }
              trailingContent={<Radio value="option3" aria-label="Label 3" />}
              onPress={() => handlePress('option3')}
            />
          </Card>
        </List>
      </RadioGroup>
    </Box>
  );
};
```

For more information on the `Checbox` component, see the [`Checkbox` component documentation](/docs/native-ui-components-checkbox--docs).

### Switch List

You can also use the list to display a list of switches. You can achieve this by using the `Switch` component as the `trailingContent` of the `ListItem`.

<UsageWrap>
  <SwitchList />
</UsageWrap>

```tsx
import React, { useState } from 'react';
import { Card, List, ListItem, ListItemIcon, Switch } from '@utilitywarehouse/native-ui';
import {
  BellMediumIcon,
  LockMediumIcon,
  LocationMediumIcon,
  EyeMediumIcon,
} from '@utilitywarehouse/react-native-icons';

const SwitchList = () => {
  const [notifications, setNotifications] = useState(false);
  const [secureId, setSecureId] = useState(false);
  const [location, setLocation] = useState(false);
  const [darkMode, setDarkMode] = useState(false);

  return (
    <List>
      <Card padding="none">
        <ListItem
          text="Notifications"
          supportingText="Allow notifications"
          divider
          leadingContent={<ListItemIcon as={BellMediumIcon} />}
          trailingContent={
            <Switch value={notifications} onValueChange={setNotifications} size="small" />
          }
          onPress={() => setNotifications(!notifications)}
        />
        <ListItem
          text="Secure ID"
          supportingText="Use secure ID"
          divider
          leadingContent={<ListItemIcon as={LockMediumIcon} />}
          trailingContent={<Switch value={secureId} onValueChange={setSecureId} size="small" />}
          onPress={() => setSecureId(!secureId)}
        />
        <ListItem
          text="Location"
          supportingText="Allow location"
          divider
          leadingContent={<ListItemIcon as={LocationMediumIcon} />}
          trailingContent={<Switch value={location} onValueChange={setLocation} size="small" />}
          onPress={() => setLocation(!location)}
        />
        <ListItem
          text="Dark mode"
          supportingText="Enable dark mode"
          divider
          leadingContent={<ListItemIcon as={EyeMediumIcon} />}
          trailingContent={<Switch value={darkMode} onValueChange={setDarkMode} size="small" />}
          onPress={() => setDarkMode(!darkMode)}
        />
      </Card>
    </List>
  );
};

export default SwitchList;
```

### List with FlatList

You can also use the `FlatList` component to render a list of items. You can achieve this by using the `FlatList` component from `react-native`,
`@utilitywarehouse/native-ui` or any other library and the `ListItem` component from `@utilitywarehouse/native-ui`.

<UsageWrap>
  <List headingText="Recent contacts" divider>
    <FlatList
      data={[
        {
          id: 'bd7acbea-c1b1-46c2-aed5-3ad53abb28ba',
          fullName: 'Aafreen Khan',
          timeStamp: '12:47 PM',
          recentText: 'Good Day!',
          avatarUrl:
            'https://images.pexels.com/photos/220453/pexels-photo-220453.jpeg?auto=compress&cs=tinysrgb&dpr=1&w=500',
        },
        {
          id: '3ac68afc-c605-48d3-a4f8-fbd91aa97f63',
          fullName: 'Sujitha Mathur',
          timeStamp: '11:11 PM',
          recentText: 'Cheer up, there!',
          avatarUrl:
            'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTyEaZqT3fHeNrPGcnjLLX1v_W4mvBlgpwxnA&usqp=CAU',
        },
        {
          id: '58694a0f-3da1-471f-bd96-145571e29d72',
          fullName: 'Anci Barroco',
          timeStamp: '6:22 PM',
          recentText: 'Good Day!',
          avatarUrl: 'https://miro.medium.com/max/1400/0*0fClPmIScV5pTLoE.jpg',
        },
        {
          id: '68694a0f-3da1-431f-bd56-142371e29d72',
          fullName: 'Aniket Kumar',
          timeStamp: '8:56 PM',
          recentText: 'All the best',
          avatarUrl:
            'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSr01zI37DYuR8bMV5exWQBSw28C1v_71CAh8d7GP1mplcmTgQA6Q66Oo--QedAN1B4E1k&usqp=CAU',
        },
        {
          id: '28694a0f-3da1-471f-bd96-142456e29d72',
          fullName: 'Kiara',
          timeStamp: '12:47 PM',
          recentText: 'I will call today.',
          avatarUrl:
            'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRBwgu1A5zgPSvfE83nurkuzNEoXs9DMNr8Ww&usqp=CAU',
        },
      ]}
      renderItem={({ item }) => (
        <ListItem
          key={item.id}
          text={item.fullName}
          supportingText={`${item.recentText} - ${item.timeStamp}`}
          leadingContent={
            <Image
              width={40}
              height={40}
              borderRadius="$md"
              backgroundColor="$grey300"
              source={{ uri: item.avatarUrl }}
            />
          }
          onPress={() => console.log('item pressed')}
        />
      )}
    />
  </List>
</UsageWrap>

```tsx
import { List, ListItem, Image, FlatList } from '@utilitywarehouse/native-ui';

const data = [
  {
    id: 'bd7acbea-c1b1-46c2-aed5-3ad53abb28ba',
    fullName: 'Aafreen Khan',
    timeStamp: '12:47 PM',
    recentText: 'Good Day!',
    avatarUrl:
      'https://images.pexels.com/photos/220453/pexels-photo-220453.jpeg?auto=compress&cs=tinysrgb&dpr=1&w=500',
  },
  {
    id: '3ac68afc-c605-48d3-a4f8-fbd91aa97f63',
    fullName: 'Sujitha Mathur',
    timeStamp: '11:11 PM',
    recentText: 'Cheer up, there!',
    avatarUrl:
      'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTyEaZqT3fHeNrPGcnjLLX1v_W4mvBlgpwxnA&usqp=CAU',
  },
  {
    id: '58694a0f-3da1-471f-bd96-145571e29d72',
    fullName: 'Anci Barroco',
    timeStamp: '6:22 PM',
    recentText: 'Good Day!',
    avatarUrl: 'https://miro.medium.com/max/1400/0*0fClPmIScV5pTLoE.jpg',
  },
  {
    id: '68694a0f-3da1-431f-bd56-142371e29d72',
    fullName: 'Aniket Kumar',
    timeStamp: '8:56 PM',
    recentText: 'All the best',
    avatarUrl:
      'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSr01zI37DYuR8bMV5exWQBSw28C1v_71CAh8d7GP1mplcmTgQA6Q66Oo--QedAN1B4E1k&usqp=CAU',
  },
  {
    id: '28694a0f-3da1-471f-bd96-142456e29d72',
    fullName: 'Kiara',
    timeStamp: '12:47 PM',
    recentText: 'I will call today.',
    avatarUrl:
      'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRBwgu1A5zgPSvfE83nurkuzNEoXs9DMNr8Ww&usqp=CAU',
  },
];

const MyComponent = () => (
  <List headingText="Recent contacts" divider>
    <FlatList
      data={data}
      renderItem={({ item }) => (
        <ListItem
          key={item.id}
          text={item.fullName}
          supportingText={`${item.recentText} - ${item.timeStamp}`}
          leadingContent={
            <Image
              width={40}
              height={40}
              borderRadius="$md"
              backgroundColor="$grey300"
              source={{ uri: item.avatarUrl }}
            />
          }
          onPress={() => console.log('item pressed')}
        />
      )}
    />
  </List>
);
```
