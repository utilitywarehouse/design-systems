import { styled, Text } from '@gluestack-ui/themed';

const ListItemSupportingText = styled(
  Text,
  {},
  {
    componentName: 'ListItemSupportingText',
    descendantStyle: [],
    ancestorStyle: ['_supportingText'],
  }
);

export default ListItemSupportingText;
