import { Meta, Canvas, Story, Primary, Controls } from '@storybook/blocks';
import * as Stories from './Card.stories';
import { Center, Card, Text, Heading } from '../../';
import { ViewFigmaButton, BackToTopButton, UsageWrap } from '../../../docs/components';

<Meta title="Components / Card" />

<ViewFigmaButton url="https://www.figma.com/design/3RY3OvLA88yZksRjOfjQJm/UW-App-UI?node-id=9344-2255&m=dev" />

<BackToTopButton />

# Card

A Card component serves as a visual container that groups related content and actions.

- [Playground](#playground)
- [Usage](#usage)
- [Props](#props)
- [Variants](#variants)

## Playground

<Canvas of={Stories.Playground} />

<Controls of={Stories.Playground} />

## Usage

<UsageWrap>
  <Center>
    <Card padding="large" colorScheme="grey" variant="outline">
      <Heading>Quick Start</Heading>
      <Text>Start building your next project in minutes.</Text>
    </Card>
  </Center>
</UsageWrap>

```jsx
import { Card, Text } from '@utilitywarehouse/native-ui';

const MyComponent = () => (
  <Card padding="large" colorScheme="grey" variant="outline">
    <Heading>Quick Start</Heading>
    <Text>Start building your next project in minutes.</Text>
  </Card>
);
```

## Props

| Property    | Type                                              | Description                      | Default     |
| ----------- | ------------------------------------------------- | -------------------------------- | ----------- |
| padding     | `'small' \| 'medium' \| 'large' \| 'none'`        | The padding size of the card.    | `'large'`   |
| variant     | `'outline' \| 'filled' \| 'elevated' \| 'dashed'` | The variant of the card.         | `'outline'` |
| colorScheme | `'base' \| 'grey' \| 'purple' `                   | The color scheme of the card.    | `'base'`    |
| surface     | `'base' \| 'purple' `                             | The surface that the card is on. | `'base'`    |
| nested      | `boolean`                                         | Whether the card is nested.      | `false`     |

## Variants

<Canvas of={Stories.Variants} />
