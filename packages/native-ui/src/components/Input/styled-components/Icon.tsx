import { styled } from '@gluestack-style/react';
import { AsForwarder } from '@gluestack-ui/themed';

export default styled(AsForwarder, {}, {
  componentName: 'InputIcon',
  ancestorStyle: ['_icon'],
} as const);
