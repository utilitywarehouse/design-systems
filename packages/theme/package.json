{
  "name": "@utilitywarehouse/customer-ui-theme",
  "version": "1.1.4",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf node_modules",
    "build": "rm -rf dist/ && tsc",
    "watch": "pnpm build -- --watch",
    "lint:fix": "eslint ./src --fix --ext .ts,.js",
    "lint": "eslint ./src --ext .ts,.js",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "src/*.ts": [
      "pnpm lint"
    ]
  },
  "license": "ISC",
  "publishConfig": {
    "access": "restricted"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@utilitywarehouse/customer-ui-design-tokens": "1.0.4",
    "@ctrl/tinycolor": "^3.4.0"
  }
}
