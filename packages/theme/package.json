{
  "name": "@utilitywarehouse/customer-ui-theme",
  "version": "1.1.3",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean:dist": "rm -rf dist/",
    "clean:deps": "rimraf node_modules",
    "clean": "pnpm clean:dist && pnpm clean:deps",
    "build": "pnpm clean:dist && tsc",
    "watch": "pnpm build -- --watch",
    "format": "prettier --write \"./src/**/*.{ts,js}\"",
    "lint": "eslint ./src --fix --ext .ts,.js",
    "format:ci": "prettier --list-different \"./src/**/*.{ts,js}\"",
    "lint:ci": "eslint ./src --ext .ts,.js",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "src/*.ts": [
      "pnpm lint"
    ]
  },
  "license": "ISC",
  "publishConfig": {
    "access": "restricted"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@utilitywarehouse/customer-ui-design-tokens": "1.0.3",
    "@ctrl/tinycolor": "^3.4.0"
  }
}
