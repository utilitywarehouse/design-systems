{
  "name": "@utilitywarehouse/customer-ui-theme",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist/",
    "build": "yarn run clean && tsc",
    "watch": "yarn run build -- --watch",
    "test": "jest --passWithNoTests",
    "ci-package-changed": "exit 0"
  },
  "license": "ISC",
  "publishConfig": {
    "access": "restricted"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@utilitywarehouse/customer-ui-design-tokens": "1.0.0-alpha.7",
    "@ctrl/tinycolor": "^3.4.0"
  }
}
