{
  "name": "@utilitywarehouse/css-colors",
  "version": "0.0.1",
  "description": "",
  "main": "./dist/index.css",
  "author": "Rob Phoenix <rphoenix@uw.co.uk>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/utilitywarehouse/design-systems.git",
    "directory": "packages/css-colors"
  },
  "scripts": {
    "clean:css": "rm -f src/css",
    "build": "postcss src --dir dist",
    "dev": "postcss --watch --verbose src --dir dist",
    "clean": "rm -rf dist && rm -rf node_modules",
    "generate": "pnpm clean:css && node ./scripts/generate-css-properties.cjs "
  },
  "publishConfig": {
    "access": "restricted"
  },
  "devDependencies": {
    "@utilitywarehouse/colour-system": "workspace:^",
    "cssnano": "^5.1.13",
    "fs-extra": "^11.1.1",
    "postcss": "^8.4.16",
    "postcss-cli": "^10.0.0",
    "prettier": "^2.2.1"
  },
  "files": [
    "README.md",
    "*.css"
  ]
}
