import React from "react";
import "./horizontalDisplayContainer.scss";

const HorizontalDisplayContainer: React.FunctionComponent = ({ children }) => (
  <div className="horizontal-display-container">{children}</div>
);

export default HorizontalDisplayContainer;
