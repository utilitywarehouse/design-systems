{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "outputs": []
    },
    "lint:fix": {
      "outputs": []
    },
    "clean": {
      "cache": false
    },
    "storybook:ci": {
      "outputs": ["storybook-build/**"]
    },
    "storybook:build": {
      "outputs": ["storybook-build/**"]
    }
  }
}
