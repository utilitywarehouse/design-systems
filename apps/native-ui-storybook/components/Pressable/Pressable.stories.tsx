import Pressable from './Pressable';
import type { Meta } from '@storybook/react';

const PressableMeta: Meta<typeof Pressable> = {
  title: 'Native UI / Components / Pressable',
  component: Pressable,
  argTypes: {},
  args: {},
};

export default PressableMeta;

export { Pressable };
