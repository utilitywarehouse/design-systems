import Divider from './Divider';
import type { Meta } from '@storybook/react';

const DividerMeta: Meta<typeof Divider> = {
  title: 'Native UI / Components / Divider',
  component: Divider,
};

export default DividerMeta;

export { Divider as Playground };
