import Skeleton from './Skeleton';
import type { Meta } from '@storybook/react';

const SkeletonMeta: Meta<typeof Skeleton> = {
  title: 'Native UI / Components / Skeleton',
  component: Skeleton,
};

export default SkeletonMeta;

export { Skeleton };
