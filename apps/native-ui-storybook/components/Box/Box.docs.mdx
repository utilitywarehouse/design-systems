import { Meta, Canvas, Story } from '@storybook/blocks';
import * as BoxStories from './Box.stories';
import { Text, NativeUIProvider, Center, Alert } from '@utilitywarehouse/native-ui';
import { Box } from '@utilitywarehouse/native-ui/lab';
import { DocComponentWrap } from '../../docs/components';

<Meta of={BoxStories} />

# Box

<DocComponentWrap>
  <Box mt="$6" mb="4">
    <Alert
      colorScheme="gold"
      text="This Box component is deprecated, use the Box from @utilitywarehouse/native-ui/lab"
    />
  </Box>
</DocComponentWrap>

> Click here to go to the [new Box component](/docs/native-ui-components-lab-box--docs).

If you're looking for a flexible component to help with low-level layout needs, the Box component might be just what you need. With its customizable styles and props, it can be used to create a wide range of UI elements, from simple containers to complex layout structures.

## Usage

> This component should be wrapped in a `NativeUIProvider` component to ensure that the correct theme is applied.

<Canvas>
  <NativeUIProvider>
    <Center>
      <Box p="$5" m="$4" bg="$grey100" borderRadius="$lg">
        <Text>Hello, world!</Text>
      </Box>
    </Center>
  </NativeUIProvider>
</Canvas>

```jsx
import { Box, Text } from '@utilitywarehouse/native-ui';

const MyComponent = () => (
  <Box p="$5" m="$4" bg="$grey100" borderRadius="$lg">
    <Text>Hello, world!</Text>
  </Box>
);
```
