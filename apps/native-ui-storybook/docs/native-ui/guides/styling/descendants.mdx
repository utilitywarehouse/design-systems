import { Meta, Canvas } from '@storybook/blocks';
import RNExample from './components/RNExample';
import GSExample from './components/GSExample';
import BackToTopButton from '../../../components/BackToTopButton';
import {
  Center,
  NativeUIProvider,
  Button,
  ButtonText,
  Box,
  Text,
} from '@utilitywarehouse/native-ui';

<Meta title="Native UI / Guides / Styling / Descendants" />

<BackToTopButton />

# Descendants

We can provide context based descendant styling with the help of gluestack-ui, i.e we can apply styles to the children of a component from the parent component.
In the below example we are applying styles to ButtonText by passing styles `_text` property in the `sx` prop of Button component.

<Canvas>
  <NativeUIProvider>
    <Center>
      <Box justifyContent="center" alignItems="center" h="100%">
        <Box alignItems="center">
          <Button $_text-color="red" backgroundColor="$grey300">
            <ButtonText>Button</ButtonText>
          </Button>
        </Box>
      </Box>
    </Center>
  </NativeUIProvider>
</Canvas>

```tsx
<Box justifyContent="center" alignItems="center" h="100%">
  <Box alignItems="center">
    <Button $_text-color="red" backgroundColor="$grey300">
      <ButtonText>Button</ButtonText>
    </Button>
  </Box>
</Box>
```
