import { Meta, Canvas } from '@storybook/blocks';
import BackToTopButton from '../../../components/BackToTopButton';
import { Center, NativeUIProvider, Box, Text } from '@utilitywarehouse/native-ui';

<Meta title="Native UI / Guides / Styling / Fonts" />

<BackToTopButton />

# Fonts

`native-ui` uses two fonts, one for headings and one for body text. The heading font is `Aeonik` and the body font is `Work Sans`.
To use these fonts with `native-ui`, unlike just using the `fontFamily` in React Native stylesheets, you can use styles similar to web CSS.

For example to use the `Work Sans` regular font, you can see the following example using a normal React Native stylesheet:

```tsx
import { View, Text, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    fontFamily: 'WorkSans-Regular',
    fontSize: 16,
  },
});

const MyComponent = () => (
  <View>
    <Text style={styles.text}>Hello World</Text>
  </View>
);
```

<Canvas>
  <NativeUIProvider>
    <Center>
      <Text
        sx={{
          fontFamily: 'Work Sans',
          fontWeight: '400',
          fontSize: 16,
        }}
      >
        Hello World
      </Text>
    </Center>
  </NativeUIProvider>
</Canvas>

Whereas with `native-ui`, you can use the following:

```tsx
import { Text, Box } from '@utilitywarehouse/native-ui';

const MyComponent = () => (
  <Box>
    <Text
      sx={{
        fontFamily: 'Work Sans',
        fontWeight: '400',
        fontSize: 16,
      }}
    >
      Hello World
    </Text>
  </Box>
);
```

<Canvas>
  <NativeUIProvider>
    <Center>
      <Text
        sx={{
          fontFamily: 'Work Sans',
          fontWeight: '400',
          fontSize: 16,
        }}
      >
        Hello World
      </Text>
    </Center>
  </NativeUIProvider>
</Canvas>

This is because `native-ui` uses a custom font loader to load the fonts into the app, and then uses the font names as they are in the font files.

## Font Weights and Aliases

To make it easier to use the font weights, we have added aliases for the font weights. To use the aliases you need to use the `fontWeight` property and pass in the weight prefixed with a `$`. For example:

```tsx
import { Text, Box } from '@utilitywarehouse/native-ui';

const MyComponent = () => (
  <Box>
    <Text
      sx={{
        fontFamily: 'Work Sans',
        fontWeight: '$regular',
        fontSize: 16,
      }}
    >
      Hello World
    </Text>
  </Box>
);
```

The `Work Sans` font has the following weights available:

| Weight      | Value | Alias         |
| ----------- | ----- | ------------- |
| Thin        | 100   | `$thin`       |
| Extra Light | 200   | `$extralight` |
| Light       | 300   | `$light`      |
| Regular     | 400   | `$regular`    |
| Medium      | 500   | `$medium`     |
| Semi Bold   | 600   | `$semibold`   |
| Bold        | 700   | `$bold`       |
| Extra Bold  | 800   | `$extrabold`  |
| Black       | 900   | `$black`      |

The `Aeonik` font has the following weights available:

| Weight  | Value | Alias      |
| ------- | ----- | ---------- |
| Regular | 400   | `$regular` |
| Bold    | 700   | `$bold`    |
